<div class="child row">
  <div class="wrapper col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">
    <h2>お知らせ</h2>
    <ul>
      <% @notifications.each do |notification| %>
      <hr>
        <li class="left">
          <span><% unless notification.read %></span>
            未読
          <span><% end %></span>
          <p class="left">
            <%= notification.comment.user.try(:name) %>さんが
            あなたのブログ（<%= link_to "#{notification.comment.blog.title}", blog_path(notification.comment.blog, notification_id: notification.id) %>）にコメントしました。
          </p>
          <p class="right"><%= posted_time(notification.created_at) %></p>
        </li>
      <% end %>
    </ul>
  </div>
</div>
